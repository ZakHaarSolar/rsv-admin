export type PlanetConfig = {
    id: string
    bgColor: string
    size: number
    title: string
    desc: string
    link: string
    targetBlank: boolean
    orbitRadius: number
    orbitDuration: number
    startAngle: number
}
